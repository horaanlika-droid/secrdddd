# Статус списка прошлой итерации · 27

15.09.2026. Список из итерации 26 разобран. Просьба этой итерации: **лицо
персонажа на главной = текущее настроение** и кнопка **«Пережить вместе»**
вместо «Открыть чат». Решение по привычкам: мимику по таймеру (моргание и
улыбку из прошлой итерации) не возвращаем — лицо показывает то, что человек
отметил.

## Сделано

- [x] Лицо на главной — последняя подтверждённая отметка дневника. Десять лиц:
      девять состояний плюс спокойное. Свежая отметка не «сгорает» за сутки:
      она на лице, пока не появится следующая (решение пользователя).
- [x] Тело, руки и капюшон — всегда `hello.png`; лицо — отдельный слой из
      `hero-moods.webp`, ячейка 416×314 ровно по контуру лица. Вне области лица
      кадр совпадает с позой до байта (проверяет и генератор, и браузерный тест).
- [x] Новой генерации нет: сетка собрана из уже готовых голов эмоций
      (`scripts/build-mascot-moods.py`), старые кадры моргания и улыбки удалены.
- [x] Лицо обновляется сразу после «Оставить запись» — без перерисовки главной,
      чтобы не терять черновик заметки, прокрутку и позицию в списке.
- [x] `prefers-reduced-motion`: лицо показывается без проявления, ничего не
      анимируется. Скринридер слышит «Дибитишка рядом · настроение: …».
- [x] Кнопка чата на главной переименована в **«Пережить вместе»** (ведёт туда
      же — в чат поддержки); подпись карточки не менялась.
- [x] Обновлены проверки: `check-project` (слой лица, источник тела, heroMood,
      геометрия, отсутствие старой сетки, новая кнопка), `dom-smoke` (48 проверок:
      спокойное лицо, лицо после отметки, отметка трёхдневной давности) и
      `browser-smoke` (одна загрузка сетки, неподвижность, «меняется только лицо»
      по пикселям, reduced-motion).

## Отменено / не расширяем без отдельного запроса

- Мимика по таймеру (моргание и улыбка) — **удалена** в итерации 27: лицо
  показывает отметку человека, а не расписание анимации.
- Кнопка «Открыть чат» на главной — **переименована** в «Пережить вместе».
  Нового «поделиться с близким» и ссылок наружу не добавляли: в прошлой
  итерации «Поделиться» убрали по прямой просьбе.

---

# Статус списка итерации 26

15.09.2026. Список из итерации 25 разобран; новые пожелания пользователя имеют
приоритет над старым планом (поэтому «Поделиться» и покачивание не возвращаем).

## Сделано

- [x] Эмоции: один сгенерированный лист **16 голов 4×4**, без плеч/тела,
      нормализация размера и прозрачный фон, нарезка в репозитории.
      Девять прежних состояний в интерфейсе + семь запасных; миграция дневника сохранена.
- [x] Компактные кнопки эмоций; выбор и заметка — только черновик.
      **«Оставить запись»** подтверждает ровно одну запись. Без выбранной эмоции
      кнопка выключена. Ошибка записи не стирает черновик и не создаёт дубль.
- [x] Маскот не плавает и не прыгает от нажатия. Тело постоянно из одного PNG
      (`hello.png`), поверх — только лицо текущего настроения (в итерации 27
      кадры мимики по таймеру заменены на выражение из дневника). Нет
      сдвига/дыхания/качания; `prefers-reduced-motion` оставляет статичное лицо.
      Браузерная проверка сравнивает пиксели тела, не только bounding box.
- [x] Одна морская иллюстрация для досок: ракушки, камушек, украшения, фантики,
      билеты, полароид с Дибитишкой у моря. Оптимизированный WebP + миниатюра.
- [x] Тёплая подсказка при пустом списке досок.
- [x] EXIF 1–8 (включая зеркальные ориентации), native `createImageBitmap` и
      fallback с нормализацией тега до 1. Все варианты проверяются по пикселям
      в Chromium; повернутый JPEG реально загружается через галерею.
- [x] IndexedDB: ошибка/abort/blocked/private-mode, понятное сообщение без
      фальшивой пустой плитки, повторная загрузка фото после фильтра и перезапуска.
- [x] Фото-фон не пропадает после удаления плитки с тем же фото; фон обложки
      подгружается и на странице списка досок. Освобождаются objectURL удалённых медиа.
- [x] Листы: отложенное закрытие старого не стирает новый, Escape и фокус,
      при редактировании заметки исходный текст заполнен.
- [x] PNG, PDF A4, предпросмотр и печать всей доски, независимо от поискового
      фильтра. Длинный текст разбивается по страницам. Кириллица отрисована
      браузером. GIF фиксируются кадром; недоступный/CORS-запрещённый файл
      заменяется явной заглушкой с предупреждением.
- [x] Синхронизация: приватный `/board`, проверка Telegram initData или сессии
      от одноразового кода, разделение аккаунтов, очередь офлайн, CAS-ревизии,
      объединение изменений и сохранение конфликтующих версий. Фото — постоянный
      диск бота или закрытое S3-совместимое хранилище. Включение и перенос
      гостевых досок — только по решению человека.
- [x] GIF-поиск через `/gif?q=` с серверным ключом Tenor, таймаутом, кешем,
      лимитом и резервным сценарием «вставить ссылку / открыть Tenor».
- [x] `scripts/dom-smoke.mjs` в репозитории и CI: реальные ES-модули в jsdom,
      48 поведенческих проверок. Отдельно — API/синхронизация/EXIF и Chromium
      с настоящими canvas, IndexedDB, загрузками PNG/PDF и мобильной вёрсткой.
- [x] Проверки `check-project.mjs` обновлены под подтверждение, единый лист,
      промежуточные кадры, экспорт, защищённый API и новые CI-команды.
- [x] Исправления узких экранов: навигационный заголовок не заходит на «Назад»,
      скрытый tabbar не оставляет белую полосу поверх досок, длинное название
      папки переносится, кнопки фона/доната не занимают всю ширину flex-ряда.

## Отменено / не расширяем без отдельного запроса

- «Поделиться» настроением — **удалено** по новому пожеланию.
- Покачивание маскота — **удалено** по новому пожеланию.
- Оживлять прелоад не стали: это необязательная идея, противоречащая направлению
  на более спокойный главный экран. Там остаётся прежняя заставка.
- Эмодзи значков/мерча не заменяли: новый запрос касается эмоций, не всех
  пиктограмм приложения. Дополнительные головы уже подготовлены в общей сетке.
- Большие старые исходники не переносили в GitHub Releases: существующие
  воспроизводимые генерации оставлены. Новые черновики/тестовые фото не в Git.

## Что нельзя подтвердить из песочницы

- [ ] **Реальные iOS Safari / Telegram WebView / Android Chrome:** системная
      галерея, HEIC конкретного телефона, sheet-фокус/клавиатура, нативная печать.
      Chromium-эмуляция и синтетические EXIF-фото не заменяют телефон.
- [ ] **Живой бот после деплоя:** вход по `/code`, два реальных устройства,
      постоянный том `DB_FILE` + `BOARDS_DATA_DIR`, права на закрытый S3-бакет,
      если выбран S3. Контракт/ошибки/изоляция проверены локально, реальные
      настройки владельца не менялись. Без постоянного диска нельзя обещать
      сохранность при пересоздании контейнера.
- [ ] **Живой Tenor:** нужен `TENOR_API_KEY` в окружении бота. Код интеграции
      и fallback проверены без живого ключа; в его отсутствие приложение
      честно предлагает вставить ссылку.

Настройка и команды — [SETUP.md](SETUP.md). Проверки запускаются
`npm run verify` и `npm run browser-test`; результаты и скриншоты — в
`test-results/` (не коммитятся).
