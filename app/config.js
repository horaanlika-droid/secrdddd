/* ============================================================
   дибитишка · конфигурация веб-приложения
   Заполни эти поля после запуска (или оставь заглушки).
   Всё остальное (токен бота, админы, Tribute API, ключ живого чата
   OPENAI_API_KEY — плюс необязательные OPENAI_MODEL / OPENAI_BASE_URL / AI_HISTORY)
   живёт в переменных окружения на бот-хосте — см. bot/README.md и docs/SETUP.md.
   В веб ключ не вписывай: запросы к OpenAI ходят через бота (POST /chat),
   без ключа чат честно работает шаблонами — заглушка уже вшита в bot/src/ai.js.
   ============================================================ */
window.DIBI_CONFIG = {
  // username твоего телеграм-бота (без @) — для кнопок «оплатить», «войти по коду».
  // ⚠️ Впиши РЕАЛЬНЫЙ username из @BotFather. Пока тут заглушка — кнопка «Оплатить
  // в Telegram» ведёт в никуда и Telegram показывает «такого пользователя
  // не существует». После правки — коммит и пуш, Pages пересоберётся за минуту.
  bot_username: "dbtrobot",

  // Ссылка мини-приложения в Telegram — именно её человек ставит на экран
  // «Домой» как иконку (подсказка в приложении всплывает один раз и живёт
  // строкой в профиле). Формат: https://t.me/<username бота>/<короткое имя
  // приложения>; короткое имя задаётся в @BotFather → /newapp и у нас это
  // всегда dibitishka (см. docs/SETUP.md, шаг 3). Если поле пустое, ссылка
  // соберётся сама из bot_username + /dibitishka.
  miniapp_url: "https://t.me/dbtrobot/dibitishka",

  // НЕОБЯЗАТЕЛЬНО: публичный URL бота, если хостинг даёт порт.
  // Тогда веб-версия получает живой контент и проверяет коды входа.
  // Пример: "https://dibi-bot.example.com" (без слэша в конце)
  bot_public_url: "https://bot-1789414185-5002-dominamillaa.bothost.tech",

  // Доступ по минимальному ежемесячному донату в Tribute. Актуальную ссылку
  // админ меняет в боте: /tribute set <ссылка>; веб ведёт в deep-link pay_m1.
  subscription: {
    price_ru: "Минимальный донат", period: "раз в месяц", trial_days: 7,
    plans: [
      { id: "m1", title: "Месяц доступа", per: "раз в месяц", price_ru: "Минимальный донат", days: 30, note: "сумма выбирается в Tribute · отмена в любой момент" }
    ]
  },

  // Донат разработчикам — разовая поддержка через Tribute (ссылка-приложение).
  // v35: обе ссылки (разовый донат и ежемесячный) ведут на один Donation Request dQui.
  donate_url: "https://t.me/tribute/app?startapp=dQui",

  // Соцсети (заглушки — замени на свои)
  social: {
    telegram: "https://t.me/dbtishka",
    email: "hello@dibitishka.app"
  }
};
