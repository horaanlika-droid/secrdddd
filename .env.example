# скопируйте в .env и заполните: cp .env.example .env
# бот в контейнере: docker compose up -d --build

# токен бота от @BotFather (обязательно)
TG_TOKEN=

# id админов через запятую (обязательно)
ADMIN_IDS=

# ключ Tribute API: кабинет Tribute → меню (три точки) → Settings → API Keys.
# Нужен, чтобы проверять подпись вебхуков (без него подписки не активируются).
TRIBUTE_API=

# ссылки на оплату из кабинета Tribute — по одной на тариф.
# Как получить: @tribute → Author Dashboard → Digital Products → Create instant product
# (три товара: «Дибитишка · 1 месяц» 200 ₽, «3 месяца» 500 ₽, «6 месяцев» 900 ₽),
# у каждого скопировать ссылку вида https://t.me/tribute/app?startapp=p123.
TRIBUTE_M1_URL=
TRIBUTE_M3_URL=
TRIBUTE_M6_URL=
# запасной вариант, если товар пока один: эта ссылка отдаётся на все тарифы
# TRIBUTE_PAY_URL=https://t.me/tribute/app?startapp=p123
# соответствие «id товара → дни подписки» (id видно в кабинете Tribute).
# Без него каждый вебхук даёт 30 дней.
# TRIBUTE_PRODUCT_DAYS=456=30,457=90,458=180
# редкий режим: динамические заказы через Shops API вместо статичных ссылок
# (нужен настроенный магазин Tribute; обычно не нужен)
# TRIBUTE_SHOP=1
# TRIBUTE_SHOP_ID=1

# ключ OpenAI — «живой» чат Дибитишки (можно позже: без него чат в локальном режиме)
OPENAI_API_KEY=
# необязательно: модель и адрес API
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1

# порт HTTP API для веб-версии (по умолчанию 8080)
# PORT=8080
